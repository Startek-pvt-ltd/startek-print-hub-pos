import { readFile } from "node:fs/promises"; import path from "node:path"; import { requirePermission } from "@/lib/auth"; import { db } from "@/lib/db"; import { generateQuotationPdf } from "@/server/quotation-pdf";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { await requirePermission("quotations:manage"); const { id } = await params; const [quotation, settings, logo] = await Promise.all([db.quotation.findUnique({ where: { id }, include: { items: { orderBy: { sortOrder: "asc" } } } }), db.setting.findUnique({ where: { id: "primary" } }), readFile(path.join(process.cwd(), "public", "brand", "startek-logo.png"))]); if (!quotation) return new Response("Quotation not found", { status: 404 }); const bytes = await generateQuotationPdf({ quotationNumber: quotation.quotationNumber, createdAt: quotation.createdAt, customerName: quotation.customerNameSnapshot, customerPhone: quotation.customerPhoneSnapshot, validUntil: quotation.validUntil, notes: quotation.notes, subtotal: quotation.subtotal.toFixed(2), discount: quotation.discount.toFixed(2), grandTotal: quotation.grandTotal.toFixed(2), business: { name: settings?.businessName ?? "Startek Print Hub", address: settings?.address ?? "No.62 Padukka Road, Meegoda", phones: [settings?.phonePrimary, settings?.phoneSecond].filter(Boolean).join(" / "), email: settings?.email ?? "startekprinthub@gmail.com" }, items: quotation.items.map((item) => ({ description: item.description, quantity: item.quantity.toString(), unitPrice: item.unitPrice.toFixed(2), lineTotal: item.lineTotal.toFixed(2) })) }, logo); return new Response(Buffer.from(bytes), { headers: { "Content-Type": "application/pdf", "Content-Disposition": `attachment; filename="${quotation.quotationNumber}.pdf"`, "Cache-Control": "private, no-store", "X-Content-Type-Options": "nosniff" } }); }
